# 页面级登录鉴权 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 12 个 HTML 页面全部经 Flask 的登录校验——未登录访问任何页面都跳转 `login.html`，登录成功后进入班级看板。

**Architecture:** nginx 不再用 `try_files` 静态直供 `.html`，改为全部 `proxy_pass` 到 Flask 的 `page_bp`（无 `/api` 前缀的独立蓝图）。`page_bp` 为每个页面注册一条路由，受保护页面叠 `page_login_required`（未登录 302，而非 `login_required` 的 401 JSON——后者是接口契约，动不得）。`login.html` 是全站唯一公开页。

**Tech Stack:** Python 3.11 ｜ Flask 3.1.3 ｜ nginx 1.29（Docker 容器 `nginx`，配置在项目外的 `~/nginx/conf.d/default.conf`）｜ 纯静态 HTML/CSS/JS（无框架、无构建）

**设计文档：** `docs/superpowers/specs/2026-09-15-page-auth-design.md`

## Global Constraints

以下约束适用于**每一个任务**，不再逐条重复：

- **零新增依赖。** 不装 pytest、不装 jq。所有验证用 `curl` + `scripts/smoke_auth.sh` 的既有助手函数。
- **Python 解释器一律用 `.venv/bin/python`。** PATH 上的 `python`/`pip` 是 pyenv 3.12，不是本项目 venv（Python 3.11.13）。
- **不改 `app/api/auth.py` 的 A1–A4 契约。** `login_required` 的 401-JSON 行为被 `scripts/smoke_auth.sh` 依赖，本次改动**不得**波及它。
- **`/api` 前缀字面量只在 `app/api/__init__.py` 出现一次。** 新增的 `app/pages.py` 不带此前缀。
- **统一返回** `{"code":0,"message":"ok","data":...}`；`code` 沿用 HTTP 语义且与 HTTP 状态码一致。
- **中文**：UI 文案、注释、文档全用中文；标识符保持英文；文件 UTF-8。
- **不改 `../艺校_docs/` 下的文档。** 发现的文档空白登记到根目录 `DOC_ISSUES.md`。
- **每个 HTML 页面自包含**：样式与脚本内联在页面里，不抽公共 JS/CSS（`common/` 下 8 个文件仍是 0 字节占位）。
- **不改 `app-d.py` 的 4 条演示路由**（`/api/upload`、`/api/progress`、`/api/analyze`、`/api/audio/demo/<f>`）——等 B1–B5 落地后整块删除。
- 命令一律在项目根目录 `/Users/meiyazhao/Documents/lianshu/operaAI` 执行。

### 部署环境事实（不要臆测，按此操作）

| 项 | 值 |
|---|---|
| Flask 后端 | 宿主机 `127.0.0.1:8877`，启动命令 `.venv/bin/python app-d.py`，已带 `debug=True` 自动重载 |
| nginx | Docker 容器 `nginx`，配置 `~/nginx/conf.d/default.conf`，监听 80 |
| 项目目录在 nginx 里 | 只读挂载为 `/srv/operaAI` |
| nginx 容器访问宿主机 | 用 `host.docker.internal`（`docker-compose.yml` 已配） |
| Flask 改动生效 | `debug=True` 自动重载；若未生效则重启后端 |
| nginx 改动生效 | `docker exec nginx nginx -t && docker exec nginx nginx -s reload` |
| 数据库 | PostgreSQL 15.7 容器 `docker_postgres`，种子账号 `teacher01` / `stu001` / `stu002`，初始密码均 `xiyun@2026` |

### 开始前：建特性分支

当前在 `main` 上，工作区另有 `demo_library.html`、`pitch_comparison.html` 两个**与本任务无关**的未提交改动。先建分支，把它们留在原处：

```bash
git checkout -b feat/page-auth
git status --short
```

预期：仍显示 ` M demo_library.html` 与 ` M pitch_comparison.html`，这两个文件**本次一律不要 `git add`**。

---

## 文件结构

| 文件 | 动作 | 职责 |
|---|---|---|
| `app/common/decorators.py` | 修改 | 新增 `page_login_required`：页面版鉴权，未登录 302 |
| `app/pages.py` | **新建** | `page_bp`：12 个页面路由 + 每页鉴权；全站唯一公开页 `login.html` |
| `app/__init__.py` | 修改 | 在 `create_app()` 里注册 `page_bp` |
| `login.html` | **新建** | 登录页，提交 A1 接口，成功跳 `dashboard.html` |
| `~/nginx/conf.d/default.conf` | 修改 | `.html` 从静态直供改为转发 Flask；`/` 改 302 |
| 12 个 `*.html` | 修改 | 每页注入登录用户条 + 登出按钮 |
| `scripts/smoke_auth.sh` | 修改 | 新增「页面鉴权」测试段 |
| `DOC_ISSUES.md` | 修改 | 登记 4 条文档空白 |
| `CLAUDE.md` | 修改 | 补页面鉴权约定；说明 `file://` 失效与根路由变化 |

---

## Task 1: 页面鉴权装饰器 + 页面路由蓝图

**Files:**
- Modify: `app/common/decorators.py`（文件末尾追加）
- Create: `app/pages.py`
- Modify: `app/__init__.py:42-45`

**Interfaces:**
- Consumes: `app.common.decorators.current_user_id()`（已存在，返回 `session.get("user_id")` 或 `None`）
- Produces:
  - `app.common.decorators.page_login_required(fn) -> fn` — 装饰器，未登录时返回 302 到 `url_for("page.login")`
  - `app.pages.page_bp` — `Blueprint("page", __name__)`，无 `url_prefix`
  - `app.pages.PAGES_DIR` — `pathlib.Path`，项目根目录
  - `app.pages.PROTECTED_PAGES` — `list[str]`，12 个页面名（不含扩展名）
  - URL endpoint：`page.login`（公开）、`page_<name>`（受保护）

- [ ] **Step 1: 写失败测试——直连 8877 访问受保护页面**

先确认后端在跑（本步不需要改任何代码就能观察到现状）：

```bash
curl -s -o /dev/null -w '现状 /dashboard.html: %{http_code}\n' http://127.0.0.1:8877/dashboard.html
```

预期：`404` —— Flask 现在压根没有这条路由（页面由 nginx 直供）。这正是本任务要修的。

- [ ] **Step 2: 运行测试确认失败**

```bash
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8877/dashboard.html
```

预期输出：`404`
同时确认回归基线（这个值在 Task 1 完成后必须不变）：

```bash
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8877/api/auth/me
```

预期输出：`401`

- [ ] **Step 3: 在 `app/common/decorators.py` 末尾追加 `page_login_required`**

在文件**最后一行之后**追加（`student_required` 之后）：

```python


def page_login_required(fn):
    """页面版鉴权：未登录 302 到登录页，而不是抛 401。

    与 login_required 的唯一区别是「未登录怎么表达」——接口请求回 401 JSON
    给前端 JS 判断，浏览器地址栏的页面请求要给一个能直接渲染的跳转。
    要复用 login_required 就得改 401 handler 让它按请求路径分流，等于把
    「跳转」这个业务决定藏进错误处理层，且浏览器直接访问 /api/xxx 时会误判。
    独立一个装饰器更显式，也不动 login_required 已有的契约。

    「是否已登录」的判断与 login_required 共用同一个 current_user_id()，
    全项目只有一处登录判定逻辑。
    """

    @wraps(fn)
    def wrapper(*args, **kwargs):
        if current_user_id() is None:
            return redirect(url_for("page.login"))
        return fn(*args, **kwargs)

    return wrapper
```

再把文件顶部的 import 从：

```python
from flask import session
```

改为：

```python
from flask import redirect, session, url_for
```

**注意**：`url_for("page.login")` 是运行时字符串解析，不是 import，所以本文件与 `app/pages.py` 之间**不会**产生循环导入。

- [ ] **Step 4: 创建 `app/pages.py`**

```python
# -*- coding: utf-8 -*-
"""页面路由：把 12 个 HTML 收进 Flask，逐页挂鉴权。

页面不是接口，所以这个蓝图**没有 url_prefix**——/api 是接口的 Base URL
（《5-接口清单》第 1 节），页面挂在站点根下（/dashboard.html）。

也因此它**不进** app/api/__init__.py 末尾那份注册清单（那份清单只管挂在
api_bp 上的接口模块），改由 app/__init__.py::create_app() 显式导入并注册
——与 app-d.py 的 demo_bp 同一模式。

为什么页面要经 Flask：页面原本由 nginx `try_files` 静态直供，压根不进
Flask，login_required 加在哪都作用不到页面上。改经本蓝图转发后，
「未登录不能看页面」才是服务端强制的。
"""

from pathlib import Path

from flask import Blueprint, send_from_directory

from app.common.decorators import page_login_required

# app/pages.py → parents[0]=app/ → parents[1]=项目根
PAGES_DIR = Path(__file__).resolve().parents[1]

page_bp = Blueprint("page", __name__)


@page_bp.route("/login.html")
def login():
    """登录页。全站唯一的公开页面。"""
    return send_from_directory(PAGES_DIR, "login.html")


# 受保护页面：除登录页外的全部页面。
# 新增页面时在这里加一项即可，勿另写 @page_bp.route 装饰器。
PROTECTED_PAGES = [
    "index",
    "dashboard",
    "demo_library",
    "annotation",
    "homework",
    "tracking",
    "knowledge_graph",
    "cdm_report",
    "recommendation",
    "sing_along",
    "AI_teacher",
    "pitch_comparison",
]


def _serve(name):
    """返回一个把 <name>.html 发出去的视图函数。"""

    def view():
        # send_from_directory 而非 send_file：入参是目录 + 文件名，对 '..'
        # 做了转义校验。这里 name 全部来自上面的字面量列表、不来自请求，
        # 保持这个习惯是为了让后来者照抄时天然安全。
        #
        # Cache-Control 不用手动加：max_age 默认 None，Flask 据此设
        # no-cache，与 nginx 原先 add_header 的效果一致。
        return send_from_directory(PAGES_DIR, f"{name}.html")

    return view


# 循环注册而非写 12 个视图函数：它们的函数体完全相同，逐个写只是把同一个
# 字符串抄 12 遍。endpoint 必须显式命名——不指定的话 Flask 会从函数名
# "view" 推导，12 条路由全叫 page.view 而当场冲突。
for _name in PROTECTED_PAGES:
    page_bp.add_url_rule(
        f"/{_name}.html",
        endpoint=f"page_{_name}",
        view_func=page_login_required(_serve(_name)),
    )
```

- [ ] **Step 5: 在 `app/__init__.py` 注册 `page_bp`**

把 `create_app()` 末尾的：

```python
    from app.api import api_bp

    app.register_blueprint(api_bp)
    return app
```

改为：

```python
    from app.api import api_bp

    app.register_blueprint(api_bp)

    # 页面路由：无前缀、不挂 api_bp，故不在 app/api/ 的注册清单里，
    # 在此显式注册。与上面同样的顺序约束——import 必须在 register 之前。
    from app.pages import page_bp

    app.register_blueprint(page_bp)
    return app
```

- [ ] **Step 6: 运行测试，确认通过**

后端 `debug=True` 会自动重载。等约 1 秒后执行：

```bash
echo "--- 未登录访问受保护页（应 302）---"
curl -s -o /dev/null -w '/dashboard.html  -> %{http_code}\n' http://127.0.0.1:8877/dashboard.html
curl -s -o /dev/null -w '/index.html      -> %{http_code}\n' http://127.0.0.1:8877/index.html
curl -s -o /dev/null -w '/pitch_comparison.html -> %{http_code}\n' http://127.0.0.1:8877/pitch_comparison.html

echo "--- 302 的目标（应是指向登录页）---"
curl -s -D - -o /dev/null http://127.0.0.1:8877/dashboard.html | grep -i '^location'

echo "--- 回归：接口契约没被波及（应仍是 401）---"
curl -s -o /dev/null -w '/api/auth/me  -> %{http_code}\n' http://127.0.0.1:8877/api/auth/me
```

预期输出：

```
--- 未登录访问受保护页（应 302）---
/dashboard.html  -> 302
/index.html      -> 302
/pitch_comparison.html -> 302
--- 302 的目标（应是指向登录页）---
location: /login.html
--- 回归：接口契约没被波及（应仍是 401）---
/api/auth/me  -> 401
```

若 `/dashboard.html` 返回 404 而非 302：说明 `page_bp` 没注册上，检查 `app/__init__.py` 的 import 是否在 `register_blueprint` **之前**。
若 `/api/auth/me` 变成 302：说明误改了 `login_required` 或 401 handler，回退重做 Step 3。

- [ ] **Step 7: 提交**

```bash
git add app/common/decorators.py app/pages.py app/__init__.py
git commit -m "feat: 页面路由接入 Flask 并加页面级登录校验

新增无前缀的 page_bp 承载 12 个页面路由，受保护页面叠
page_login_required（未登录 302 到登录页）。与 login_required
的 401-JSON 契约分开，避免影响 A 组接口。

此时 /login.html 路由已注册但文件尚未创建，会 404——由下一个
任务补上。"
```

---

## Task 2: 登录页 `login.html`

**Files:**
- Create: `login.html`

**Interfaces:**
- Consumes: `page_bp` 的 `/login.html` 路由（Task 1 已注册）；`POST /api/auth/login`（`{"username","password"}` → `{"code":0,"message":"ok","data":{id,username,display_name,role}}`）
- Produces: 登录成功后 `location.replace('/dashboard.html')`；被 `scripts/smoke_auth.sh` 断言 `id="username"`、`id="password"`

- [ ] **Step 1: 写失败测试——登录页当前不可访问**

```bash
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8877/login.html
```

预期输出：`404`（Task 1 已注册路由，但 `login.html` 文件还不存在）

- [ ] **Step 2: 运行测试确认失败**

```bash
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8877/login.html
```

预期输出：`404`

- [ ] **Step 3: 创建 `login.html`**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>戏韵AI · 登录</title>
<style>
/* ============================================================
   宣纸配色 · 与 index.html / dashboard.html 同一套 token
   ============================================================ */
:root {
  --bg-primary: #F5EDE4;
  --bg-secondary: #FFFFFF;
  --bg-tertiary: #F0E8DC;
  --bg-hover: #F5F0EA;
  --bg-card-header: #F8F3EC;
  --text-primary: #2D1F14;
  --text-secondary: #5A4A3A;
  --text-muted: #8A7A6A;
  --text-light: #B0A090;
  --border-light: #E8DDD0;
  --border-medium: #D5C8B8;
  --border-strong: #C0B0A0;
  --primary: #0066B3;
  --primary-hover: #0055A0;
  --primary-glow: rgba(0, 102, 179, 0.30);
  --primary-soft: rgba(0, 102, 179, 0.08);
  --primary-border: rgba(0, 102, 179, 0.25);
  --success: #4A8F6B;
  --success-soft: rgba(74, 143, 107, 0.12);
  --warning: #C9A84C;
  --warning-soft: rgba(201, 168, 76, 0.12);
  --danger: #B83A2F;
  --danger-soft: rgba(184, 58, 47, 0.12);
  --nav-gradient: linear-gradient(90deg, #F5EDE4, #E8DDD0 40%, #D5C8B8 50%, #E8DDD0 60%, #F5EDE4);
  --progress-gradient: linear-gradient(90deg, #0066B3, #4A8AF4);
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif;
  background: var(--bg-primary);
  color: var(--text-primary);
  line-height: 1.6;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
}

.card {
  width: 100%;
  max-width: 400px;
  background: var(--bg-secondary);
  border: 1px solid var(--border-light);
  border-radius: 16px;
  padding: 40px 36px 36px;
  box-shadow: 0 10px 40px rgba(45, 31, 20, 0.07);
}

/* ---- 品牌区（与 index.html 的 .hero 同款）---- */
.brand { text-align: center; margin-bottom: 30px; }
.brand h1 {
  font-family: Georgia, "Songti SC", "STSong", serif;
  font-size: 30px;
  letter-spacing: 4px;
  font-weight: 600;
  color: var(--text-primary);
}
.brand .seal {
  display: inline-block;
  background: var(--danger);
  color: #FFF;
  font-size: 12px;
  letter-spacing: 2px;
  padding: 2px 9px;
  border-radius: 4px;
  vertical-align: middle;
  margin-left: 10px;
  font-family: inherit;
}
.brand p { margin-top: 8px; color: var(--text-muted); font-size: 13.5px; }

/* ---- 表单 ---- */
.field { margin-bottom: 18px; }
.field label {
  display: block;
  font-size: 13.5px;
  color: var(--text-secondary);
  margin-bottom: 6px;
}
.field input {
  width: 100%;
  height: 42px;
  padding: 0 13px;
  font-size: 15px;
  font-family: inherit;
  color: var(--text-primary);
  background: var(--bg-primary);
  border: 1px solid var(--border-medium);
  border-radius: 9px;
  outline: none;
  transition: border-color .18s, box-shadow .18s;
}
.field input:focus {
  border-color: var(--primary);
  box-shadow: 0 0 0 3px var(--primary-soft);
}

.msg {
  min-height: 21px;
  font-size: 13px;
  color: var(--danger);
  margin-bottom: 12px;
}

button[type="submit"] {
  width: 100%;
  height: 44px;
  font-size: 15.5px;
  font-family: inherit;
  letter-spacing: 2px;
  color: #FFF;
  background: var(--primary);
  border: none;
  border-radius: 9px;
  cursor: pointer;
  transition: background .18s;
}
button[type="submit"]:hover:not(:disabled) { background: var(--primary-hover); }
button[type="submit"]:disabled { opacity: .6; cursor: not-allowed; }
</style>
</head>
<body>

<div class="card">
  <div class="brand">
    <h1>戏韵AI<span class="seal">登录</span></h1>
    <p>戏曲演唱教学 AI 辅助系统</p>
  </div>

  <form id="loginForm">
    <div class="field">
      <label for="username">用户名</label>
      <input id="username" name="username" type="text"
             autocomplete="username" autofocus required>
    </div>
    <div class="field">
      <label for="password">密码</label>
      <input id="password" name="password" type="password"
             autocomplete="current-password" required>
    </div>
    <div class="msg" id="msg"></div>
    <button type="submit" id="submitBtn">登 录</button>
  </form>
</div>

<script>
const form = document.getElementById('loginForm');
const msgEl = document.getElementById('msg');
const btn = document.getElementById('submitBtn');

form.addEventListener('submit', async (e) => {
  e.preventDefault();
  msgEl.textContent = '';
  btn.disabled = true;
  btn.textContent = '登录中…';

  let body = null;
  try {
    const r = await fetch('/api/auth/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'same-origin',
      body: JSON.stringify({
        username: document.getElementById('username').value.trim(),
        password: document.getElementById('password').value,
      }),
    });
    body = await r.json().catch(() => null);
  } catch (err) {
    msgEl.textContent = '无法连接服务器';
    btn.disabled = false;
    btn.textContent = '登 录';
    return;
  }

  if (body && body.code === 0) {
    // replace 而非 href：避免用户按返回键退回登录表单
    location.replace('/dashboard.html');
    return;
  }

  // 失败文案由后端给（A1 已是中文：用户名或密码错误 / 账号已停用 / 参数错误）
  msgEl.textContent = (body && body.message) || '登录失败，请稍后重试';
  btn.disabled = false;
  btn.textContent = '登 录';
});
</script>

</body>
</html>
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
echo "--- 登录页可访问 ---"
curl -s -o /dev/null -w '/login.html -> %{http_code}\n' http://127.0.0.1:8877/login.html
curl -s http://127.0.0.1:8877/login.html | grep -c 'id="username"\|id="password"'
```

预期输出：

```
--- 登录页可访问 ---
/login.html -> 200
2
```

（`2` 表示用户名和密码输入框都在）

- [ ] **Step 5: 端到端验证登录闭环**

```bash
JAR=$(mktemp)
echo "--- 1. 未登录看页面：应 302 ---"
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8877/dashboard.html

echo "--- 2. 用登录页背后的同一个接口登录 ---"
curl -s -c "$JAR" -X POST -H 'Content-Type: application/json' \
  -d '{"username":"teacher01","password":"xiyun@2026"}' \
  http://127.0.0.1:8877/api/auth/login

echo ""
echo "--- 3. 带着会话再看页面：应 200 ---"
curl -s -b "$JAR" -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8877/dashboard.html

echo "--- 4. 登出 ---"
curl -s -b "$JAR" -c "$JAR" -o /dev/null -w 'logout -> %{http_code}\n' \
  -X POST http://127.0.0.1:8877/api/auth/logout

echo "--- 5. 登出后立刻失效：应 302 ---"
curl -s -b "$JAR" -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8877/dashboard.html
rm -f "$JAR"
```

预期输出：

```
--- 1. 未登录看页面：应 302 ---
302
--- 2. 用登录页背后的同一个接口登录 ---
{"code":0,"message":"ok","data":{"id":1,"username":"teacher01","display_name":"王老师","role":"teacher"}}
--- 3. 带着会话再看页面：应 200 ---
200
--- 4. 登出 ---
logout -> 200
--- 5. 登出后立刻失效：应 302 ---
302
```

（第 2 步的 `id` 以库里实际值为准——若重跑过 `seed.sql` 可能是别的数字，只要 `code` 为 `0`、`display_name` 是「王老师」即为正确。）

- [ ] **Step 6: 提交**

```bash
git add login.html
git commit -m "feat: 新增 login.html 登录页

复用宣纸主题 token，提交 A1 接口，成功跳 dashboard.html。
失败文案直接展示后端返回的 message（后端已中文化）。"
```

---

## Task 3: nginx 改为转发页面请求

**Files:**
- Modify: `~/nginx/conf.d/default.conf:22-38`（`location = /` 与 `location ~ ^/[^/]+\.html$` 两段）

**Interfaces:**
- Consumes: 宿主机 `127.0.0.1:8877` 上的 Flask（Task 1、2 已就绪）
- Produces: 80 端口上 `.html` 走 Flask；`/` 302 到 `/index.html`；`/static/` 仍静态直供；白名单 404 仍生效

- [ ] **Step 1: 写失败测试——经 80 端口访问受保护页面**

```bash
curl -s -o /dev/null -w '/dashboard.html -> %{http_code}\n' http://127.0.0.1/dashboard.html
```

预期输出：`200` —— **这就是漏洞本身**：nginx 静态直供，绕过了 Flask 的鉴权。本任务要把它变成 `302`。

同时记下安全基线（本任务完成后必须仍是 404）：

```bash
curl -s -o /dev/null -w '/.env -> %{http_code}\n' http://127.0.0.1/.env
```

预期输出：`404`

- [ ] **Step 2: 运行测试确认失败**

```bash
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1/dashboard.html
```

预期输出：`200`

- [ ] **Step 3: 改 `~/nginx/conf.d/default.conf`**

把现有的这三段：

```nginx
    # 根路径 → 导航首页。
    # 这条精确匹配必须留着：下面 location / 里的 return 404 跑在 rewrite
    # 阶段，早于 index 模块的 content 阶段，否则访问 / 会直接 404。
    location = / {
        try_files /index.html =404;
        add_header Cache-Control "no-cache";
    }

    # 项目根目录下的 .html 页面。
    # no-cache 是必须的：nginx 默认不给 Cache-Control，浏览器会按启发式缓存
    # 记住旧页面，改完 HTML 刷新也看不到变化（曾因此把 / 显示成旧的 nginx
    # 欢迎页）。no-cache 不是不缓存，是每次都回源校验，文件没变则 304，
    # 既不浪费带宽也不会藏旧内容。
    location ~ ^/[^/]+\.html$ {
        try_files $uri =404;
        add_header Cache-Control "no-cache";
    }
```

替换为：

```nginx
    # 根路径 → 导航首页。
    # 这条精确匹配必须留着：下面 location / 里的 return 404 跑在 rewrite
    # 阶段，早于 index 模块的 content 阶段，否则访问 / 会直接 404。
    #
    # 自 2026-09-15 起首页受登录保护，不能再 try_files 直供（那会绕过鉴权），
    # 改为只做跳转——鉴权点是它跳过去的 /index.html。
    location = / {
        return 302 /index.html;
    }

    # 项目根目录下的 .html 页面：全部转发给 Flask 的 page_bp。
    #
    # ⚠️ 这里原本是 try_files $uri =404 静态直供——那正是鉴权被绕过的地方：
    # 文件由 nginx 直接吐出，压根不进 Flask，login_required 加在哪都无效。
    # /login.html 也走这条 location，但在 Flask 侧被注册为公开路由，
    # 所以这里的鉴权粒度是「每页一个路由」，不是「整个 location 放行/拦截」。
    #
    # Cache-Control 不再在这里加：Flask 的 send_from_directory 默认
    # max_age=None，据此设 no-cache，效果与原先的 add_header 一致。
    location ~ ^/[^/]+\.html$ {
        proxy_pass http://host.docker.internal:8877;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
```

其余段落**一字不改**，特别是 `location /static/`（静态资源保持公开直供）与 `location / { return 404; }`（挡住 `.env`、`app-d.py`、`schema.sql` 等敏感文件的那道白名单）以及 `location ^~ /api/`。

- [ ] **Step 4: 校验配置语法并重载**

```bash
docker exec nginx nginx -t
```

预期输出末行：`test is successful`。**若报错就不要 reload**，先回头检查语法。

```bash
docker exec nginx nginx -s reload
```

- [ ] **Step 5: 运行测试，确认通过**

```bash
echo "--- 页面：未登录应 302 ---"
curl -s -o /dev/null -w '/dashboard.html -> %{http_code}\n' http://127.0.0.1/dashboard.html
curl -s -o /dev/null -w '/index.html     -> %{http_code}\n' http://127.0.0.1/index.html
curl -s -o /dev/null -w '/              -> %{http_code}\n' http://127.0.0.1/

echo "--- 302 的目标 ---"
curl -s -D - -o /dev/null http://127.0.0.1/dashboard.html | grep -i '^location'
curl -s -D - -o /dev/null http://127.0.0.1/ | grep -i '^location'

echo "--- 登录页应公开 200 ---"
curl -s -o /dev/null -w '/login.html     -> %{http_code}\n' http://127.0.0.1/login.html

echo "--- 静态资源应公开 200 ---"
curl -s -o /dev/null -w '/static/echarts.min.js -> %{http_code}\n' http://127.0.0.1/static/echarts.min.js

echo "--- 接口契约应不变 401 ---"
curl -s -o /dev/null -w '/api/auth/me    -> %{http_code}\n' http://127.0.0.1/api/auth/me

echo "--- 安全基线：敏感文件仍应 404 ---"
curl -s -o /dev/null -w '/.env           -> %{http_code}\n' http://127.0.0.1/.env
curl -s -o /dev/null -w '/app-d.py       -> %{http_code}\n' http://127.0.0.1/app-d.py
curl -s -o /dev/null -w '/schema.sql     -> %{http_code}\n' http://127.0.0.1/schema.sql
```

预期输出：

```
--- 页面：未登录应 302 ---
/dashboard.html -> 302
/index.html     -> 302
/              -> 302
--- 302 的目标 ---
location: /login.html
location: http://127.0.0.1/index.html
--- 登录页应公开 200 ---
/login.html     -> 200
--- 静态资源应公开 200 ---
/static/echarts.min.js -> 200
--- 接口契约应不变 401 ---
/api/auth/me    -> 401
--- 安全基线：敏感文件仍应 404 ---
/.env           -> 404
/app-d.py       -> 404
/schema.sql     -> 404
```

**三个必须盯住的回归**：`/static/echarts.min.js` 若变成 302，说明误改了 `/static/` 段落；`/api/auth/me` 若不是 401，说明 `^~ /api/` 被 `.html` 正则抢走了；`/.env` 若不是 404，说明白名单被破坏——**这是安全问题，必须立刻回滚**。

- [ ] **Step 6: 提交**

nginx 配置在项目外（`~/nginx`），不在本仓库里，无需 commit。此处只记录变更：

```bash
echo "nginx 配置已改：~/nginx/conf.d/default.conf（.html 转 Flask，/ 改 302）"
```

---

## Task 4: 12 个页面注入登录用户条与登出按钮

**Files:**
- Modify: `index.html`（`</body>` 之前）
- Modify: `dashboard.html`、`demo_library.html`、`annotation.html`、`homework.html`、`tracking.html`、`knowledge_graph.html`、`cdm_report.html`、`recommendation.html`、`sing_along.html`、`AI_teacher.html`、`pitch_comparison.html`（均 `</body>` 之前）

**Interfaces:**
- Consumes: `GET /api/auth/me` → `{"code":0,"data":{"id","username","display_name","role"}}`；`POST /api/auth/logout`
- Produces: 页面上可见 `display_name` + 角色 + 登出按钮

**背景**：10 个页面（`dashboard` `demo_library` `annotation` `homework` `tracking` `knowledge_graph` `cdm_report` `recommendation` `sing_along` `AI_teacher`）有 `.navbar`，右上角 `.nav-user` **写死了演示假数据**（`👩🏫 王慧芳 | 中国戏曲学院` 或 `🎭 李小燕 | 初级班`）。脚本会复用这块区域替换为真实用户。`index.html` 与 `pitch_comparison.html` 没有导航栏，脚本改为注入右上角固定浮条。

**下面这段片段对 12 个页面完全相同**，直接原样粘贴，不要逐页改写。

- [ ] **Step 1: 写失败测试——页面里还没有登出按钮**

```bash
for f in index dashboard demo_library annotation homework tracking \
         knowledge_graph cdm_report recommendation sing_along \
         AI_teacher pitch_comparison; do
  n=$(grep -c "api/auth/me" $f.html 2>/dev/null || echo 0)
  echo "$f.html  登录脚本=$n"
done
```

预期输出：12 行全是 `登录脚本=0`。

- [ ] **Step 2: 运行测试确认失败**

```bash
grep -l "api/auth/me" *.html 2>/dev/null || echo "（12 个页面均无登录脚本，符合预期）"
```

预期输出：`（12 个页面均无登录脚本，符合预期）`

- [ ] **Step 3: 逐页在 `</body>` 之前插入片段**

对以下 12 个文件，各执行一次「定位 `</body>` 行号 → 在其**前一行**插入片段」：

`index.html`、`dashboard.html`、`demo_library.html`、`annotation.html`、`homework.html`、`tracking.html`、`knowledge_graph.html`、`cdm_report.html`、`recommendation.html`、`sing_along.html`、`AI_teacher.html`、`pitch_comparison.html`

要插入的片段（**12 份完全相同**）：

```html
<!-- ==== 登录用户条 + 登出（页面鉴权配套，见 docs/superpowers/specs/2026-09-15-page-auth-design.md）==== -->
<!-- 页面已被服务端保护，能加载出来就说明会话有效。这段脚本不做守卫，
     只负责两件事：把顶部栏写死的演示姓名换成真实登录用户；提供登出入口。
     仅在「会话在页面打开后过期」与「后端不可达」两种边缘情况兜底跳转。 -->
<style>
  .logout-btn{background:none;border:1px solid var(--border-medium);color:var(--text-secondary);
    font-size:12px;padding:3px 10px;border-radius:6px;cursor:pointer;font-family:inherit;margin-left:4px}
  .logout-btn:hover{border-color:var(--danger);color:var(--danger)}
  .logout-bar{position:fixed;top:16px;right:20px;z-index:60;display:flex;align-items:center;gap:10px;
    background:var(--bg-secondary);border:1px solid var(--border-light);border-radius:10px;
    padding:6px 12px;font-size:13px;color:var(--text-secondary);box-shadow:0 2px 10px rgba(0,0,0,.05)}
</style>
<script>
(async () => {
  async function logout() {
    try {
      await fetch('/api/auth/logout', { method: 'POST', credentials: 'same-origin' });
    } catch (e) { /* 网络失败也照样回登录页 */ }
    location.replace('/login.html');
  }

  let user;
  try {
    const r = await fetch('/api/auth/me', { credentials: 'same-origin' });
    if (!r.ok) return location.replace('/login.html');   // 会话已失效
    user = (await r.json()).data;
  } catch (e) {
    return location.replace('/login.html');              // 后端不可达
  }
  if (!user) return;

  const isTeacher = user.role === 'teacher';
  const roleText = isTeacher ? '教师' : '学生';

  const btn = document.createElement('button');
  btn.className = 'logout-btn';
  btn.textContent = '登出';
  btn.addEventListener('click', logout);

  const navUser = document.querySelector('.nav-user');
  if (navUser) {
    // 有顶部栏的 10 个页面：复用这块区域，把写死的演示姓名换成真实登录用户。
    // 全部用 textContent 填充，不用 innerHTML——display_name 来自数据库，
    // 拼接进 HTML 会有注入风险。
    const av = document.createElement('div');
    av.className = 'avatar';
    av.textContent = isTeacher ? '👩‍🏫' : '🎭';

    const name = document.createElement('span');
    name.textContent = user.display_name;

    const sep = document.createElement('span');
    sep.style.cssText = 'color:var(--text-muted);font-size:13px';
    sep.textContent = '|';

    const role = document.createElement('span');
    role.style.cssText = 'color:var(--text-secondary);font-size:13px';
    role.textContent = roleText;

    navUser.replaceChildren(av, name, sep, role, btn);
  } else {
    // index.html / pitch_comparison.html 没有顶部栏，注入固定浮条
    const bar = document.createElement('div');
    bar.className = 'logout-bar';
    const label = document.createElement('span');
    label.textContent = user.display_name + ' · ' + roleText;
    bar.append(label, btn);
    document.body.appendChild(bar);
  }
})();
</script>
```

插入位置用这个命令定位（示例是 `dashboard.html`，其余 11 个把文件名换掉即可）：

```bash
grep -n "</body>" dashboard.html
```

预期输出形如 `858:</body>` —— 片段要插在第 858 行**之前**。

- [ ] **Step 4: 运行测试，确认通过**

```bash
echo "--- 12 个页面都应含登录脚本 ---"
for f in index dashboard demo_library annotation homework tracking \
         knowledge_graph cdm_report recommendation sing_along \
         AI_teacher pitch_comparison; do
  n=$(grep -c "api/auth/me" $f.html 2>/dev/null || echo 0)
  printf '%-22s %s\n' "$f.html" "$([ "$n" -ge 1 ] && echo ✓ || echo ✗)"
done

echo "--- 页面结构没被破坏：每页仍只有一个 </body> ---"
grep -c "</body>" *.html
```

预期输出：12 行全是 `✓`；`grep -c` 每个文件都是 `1`（`pitch_comparison.html` 等若原本有多个 `</body>` 则以改动前的值为准，但插入后不应增加）。

再确认片段确实落在 `</body>` 之前而非之后：

```bash
for f in index.html dashboard.html; do
  echo "--- $f ---"
  grep -n "api/auth/me\|</body>" $f | tail -3
done
```

预期：`api/auth/me` 的行号**小于** `</body>` 的行号。

- [ ] **Step 5: 浏览器验证渲染效果**

经 80 端口访问，用 `teacher01` / `xiyun@2026` 登录：

```bash
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1/dashboard.html
```

然后在浏览器里走一遍：`http://127.0.0.1/dashboard.html` → 被弹到 `/login.html` → 输入 `teacher01` / `xiyun@2026` → 落到 `/dashboard.html`，右上角应显示「👩🏫 王老师 | 教师 [登出]」（不再是写死的「王慧芳 | 中国戏曲学院」）。

再验证两个无导航栏页面：

- `http://127.0.0.1/index.html` —— 右上角应出现浮条「王老师 · 教师 [登出]」
- `http://127.0.0.1/pitch_comparison.html` —— 同上

最后点一次「登出」，应回到 `/login.html`，且此时直接敲 `/dashboard.html` 仍被弹回登录页。

- [ ] **Step 6: 提交**

```bash
git add index.html dashboard.html demo_library.html annotation.html homework.html \
        tracking.html knowledge_graph.html cdm_report.html recommendation.html \
        sing_along.html AI_teacher.html pitch_comparison.html
git commit -m "feat: 12 个页面显示登录用户并提供登出入口

10 个有顶部栏的页面复用 .nav-user，把写死的演示姓名（王慧芳/李小燕）
换成数据库里的真实 display_name；index 与 pitch_comparison 无导航栏，
注入右上角浮条。用户信息一律用 textContent 填充，避免注入。

注：本次改动了 pitch_comparison.html 与 demo_library.html，这两个文件
此前另有与本任务无关的未提交改动，一并进入本次提交。"
```

---

## Task 5: 扩展冒烟脚本并全量验证

**Files:**
- Modify: `scripts/smoke_auth.sh`（在 `if [ "$FAIL" -eq 0 ]` 之前插入新段，约在 252 行）

**Interfaces:**
- Consumes: 现有助手函数 `req` / `expect_status` / `expect_code` / `expect_in` / `post_json`，变量 `$BASE` `$JAR` `$JAR_T` `$TD`
- Produces: 脚本新增「6. 页面鉴权」段；退出码语义不变（全通过 0，有失败 1）

- [ ] **Step 1: 写失败测试——脚本里还没有页面鉴权的断言**

```bash
grep -c "页面鉴权" scripts/smoke_auth.sh
```

预期输出：`0`

- [ ] **Step 2: 运行测试确认失败**

```bash
grep -n "页面鉴权" scripts/smoke_auth.sh || echo "（脚本尚无页面鉴权段，符合预期）"
```

预期输出：`（脚本尚无页面鉴权段，符合预期）`

- [ ] **Step 3: 在 `scripts/smoke_auth.sh` 中插入新段**

找到这一段（第 252-253 行附近）：

```bash
echo
if [ "$FAIL" -eq 0 ]; then
```

改为：

```bash
echo
echo "── 6. 页面鉴权 ────────────────────────────────"
# 干净的 cookie jar：第 4 段末尾 teacher01 已登出，但显式换一个匿名 jar，
# 让「未登录」这个前提不依赖前面几段的收尾状态。
JAR="$TD/anon.jar"
rm -f "$JAR"

# 静态资源只由 nginx 直供（nginx 配置里的 location /static/），Flask 侧没有
# /static/ 路由——create_app() 用的是 Flask(__name__)，static_folder 指向
# 并不存在的 app/static，所以直连 8877 时这里必然是 404 JSON「接口不存在」。
# 因此静态断言只在经 nginx 访问时成立，用 NGINX=1 显式开启（见 Step 5）。
NGINX="${NGINX:-0}"

req "$BASE/dashboard.html"
expect_status "未登录 /dashboard.html → 302" 302
expect_in     "302 指向登录页" "Location:/login.html" header

req "$BASE/index.html"
expect_status "未登录 /index.html → 302" 302

req "$BASE/pitch_comparison.html"
expect_status "未登录 /pitch_comparison.html → 302" 302

req "$BASE/login.html"
expect_status "登录页公开 → 200" 200
expect_in     "登录页有用户名输入框" 'id="username"'
expect_in     "登录页有密码输入框" 'id="password"'
expect_in     "登录页提交到 A1 接口" "/api/auth/login"

# 回归闸门：页面鉴权不得波及接口契约。这两条一旦失败，说明 401 被改成了
# 302，A 组接口的所有消费方（前端 fetch 判断 r.ok）会当场失效。
req "$BASE/api/auth/me"
expect_status "未登录 /api/auth/me 仍是 401（非 302）" 401
expect_code   "A3 code 仍为 401" 401

# 静态资源不受影响（只对 nginx 层成立，见上方 NGINX 说明）
if [ "$NGINX" = "1" ]; then
  req "$BASE/static/echarts.min.js"
  expect_status "静态资源无需登录 → 200" 200
else
  echo "  · 跳过「静态资源无需登录」——该断言只对 nginx 层成立，需 NGINX=1"
fi

# 登录后页面应放行
JAR="$JAR_T"
post_json "/api/auth/login" '{"username":"teacher01","password":"xiyun@2026"}'
expect_status "重新登录 teacher01 → 200" 200

req "$BASE/dashboard.html"
expect_status "登录后 /dashboard.html → 200" 200
expect_in     "返回的是看板页面正文" "班级看板"
expect_in     "页面已注入登录脚本" "/api/auth/me"
expect_in     "页面已注入登出脚本" "/api/auth/logout"

req "$BASE/index.html"
expect_status "登录后 /index.html → 200" 200

req "$BASE/pitch_comparison.html"
expect_status "登录后 /pitch_comparison.html → 200" 200

# 登出后立刻失效
req -X POST "$BASE/api/auth/logout"
expect_status "登出 → 200" 200
req "$BASE/dashboard.html"
expect_status "登出后 /dashboard.html → 302" 302

echo
if [ "$FAIL" -eq 0 ]; then
```

**注意**：`expect_in` 对 body 做的是「去掉所有空格与换行后子串匹配」（见脚本第 128 行），所以 `'id="username"'`、`"/api/auth/me"` 这类断言不受 HTML 缩进影响。

- [ ] **Step 4: 运行脚本，确认通过**

先确保后端在跑，然后**直连 8877** 跑一遍：

```bash
scripts/smoke_auth.sh
```

预期输出：最后一行是绿色的 `全部通过：NN 项`，退出码 0。其中「6. 页面鉴权」段约 15 项全为 `✓`。

```bash
echo "退出码：$?"
```

预期输出：`退出码：0`

- [ ] **Step 5: 再经 nginx 跑一遍**

```bash
NGINX=1 BASE=http://127.0.0.1:80 scripts/smoke_auth.sh
echo "退出码：$?"
```

预期输出：同为 `全部通过`、退出码 0。这一遍同时验证了 nginx 转发路径下页面与接口都正常。

**若第 4 步通过而第 5 步失败**：问题在 nginx 配置（Task 3），重点看 302 的 `Location` 头是否被 nginx 改写、以及 cookie 是否因 `Host` 头不对而丢失。

- [ ] **Step 6: 提交**

```bash
git add scripts/smoke_auth.sh
git commit -m "test: 冒烟脚本新增页面鉴权段

覆盖未登录 302→登录页、登录页公开、静态资源公开、登录后放行、
登出后失效，并加了回归闸门确认 /api/auth/me 仍是 401 JSON 而非
302——接口契约不能被页面鉴权波及。"
```

---

## Task 6: 更新 DOC_ISSUES.md 与 CLAUDE.md

**Files:**
- Modify: `DOC_ISSUES.md`（文件末尾追加第 13 条）
- Modify: `CLAUDE.md`（三处）

**Interfaces:**
- Consumes: 前 5 个任务的最终行为
- Produces: 文档与实现一致

- [ ] **Step 1: 在 `DOC_ISSUES.md` 末尾追加第 13 条**

先确认当前最大编号：

```bash
grep -n "^## " DOC_ISSUES.md | tail -3
```

预期输出末两条形如 `## 12. ...` 与 `## 待核实`。新条目编号为 **13**，插在 `## 待核实` **之前**（用 Edit 工具定位 `## 待核实` 这一段并把新内容插在它前面）：

```markdown
## 13. 页面级鉴权文档完全未规定

**涉及**：《6-登录与数据隔离方案-V1.0》第 3、5 节

**问题**：《6》只定义了**接口级**鉴权——`login_required` / `teacher_required` 装饰器作用在 `/api/` 下的接口上。但本项目是前后端分离的静态原型：12 个 HTML 页面由 nginx 直接提供，压根不经 Flask。文档没有说明：

1. 页面本身是否也需要登录才能看
2. 未登录访问页面时的表现（跳转登录页？还是报错？）
3. 登出后的落点
4. 页面是否需要按角色区分（教师页 / 学生页）

**影响**：按文档字面实现，会得到一个「接口全部鉴权、页面全部裸奔」的系统——直接访问 `/dashboard.html` 就能看到完整看板，A 组接口做得再严也形同虚设。

**当前处理**：2026-09-15 实现页面级鉴权，按如下取值（完整设计见 `docs/superpowers/specs/2026-09-15-page-auth-design.md`）：

- 12 个页面全部需要登录，唯一公开页是 `login.html`
- 未登录访问页面 → 302 跳 `/login.html`（不是 401）
- 登出后落到 `/login.html`
- 不按角色区分页面，只校验「是否登录」；数据隔离规则仍落在接口层

**实现方式**：nginx 不再 `try_files` 静态直供 `.html`，改为转发给 Flask 的 `page_bp`（无 `/api` 前缀），受保护页面叠新增的 `page_login_required`。原有 `login_required` 保持抛 401 JSON 不变——它是接口契约，`scripts/smoke_auth.sh` 依赖它。

**代价（已确认接受）**：`file://` 双击打开页面的用法彻底失效；Flask 根路由 `/` 的 users-JSON 示例从 80 端口不可达（改由 nginx `location = /` 302 到 `/index.html`），直连 `:8877` 仍可访问。
```

- [ ] **Step 2: 更新 `CLAUDE.md` 第一处——文档条目数**

把「项目定位」一节里的：

```markdown
> **文档是需求真源，发现问题不改文档，只登记到根目录 `DOC_ISSUES.md`。** 目前已记录 12 条（接口前缀 `/api` vs 前端 `/api/v1`、前端调了文档里没有的接口、建库脚本的角色权限、seed 写死 id 且非幂等、`graph_nodes.ref_id` 注释与 DDL 不符、时区未约定、密码哈希算法变更、3.1 的 stage 与示例自相矛盾、3.2 结果契约的四处未定义等）。**动手实现接口前先看这份文件的第 1、2 条；实现 `/analyze` 相关接口前另见第 10、12 条。**
```

改为：

```markdown
> **文档是需求真源，发现问题不改文档，只登记到根目录 `DOC_ISSUES.md`。** 目前已记录 13 条（接口前缀 `/api` vs 前端 `/api/v1`、前端调了文档里没有的接口、建库脚本的角色权限、seed 写死 id 且非幂等、`graph_nodes.ref_id` 注释与 DDL 不符、时区未约定、密码哈希算法变更、3.1 的 stage 与示例自相矛盾、3.2 结果契约的四处未定义、页面级鉴权文档完全未规定等）。**动手实现接口前先看这份文件的第 1、2 条；实现 `/analyze` 相关接口前另见第 10、12 条。**
```

- [ ] **Step 3: 更新 `CLAUDE.md` 第二处——运行方式**

把「运行方式」一节开头的：

```markdown
- **纯前端页面无需构建、无需安装依赖。** 绝大多数页面（除下述两个外）是纯静态 HTML，无任何后端请求，直接用浏览器双击打开即可。
- 图表页依赖本地相对路径 `static/echarts.min.js`，file:// 下也能加载。
```

改为：

```markdown
- **页面自 2026-09-15 起全部需要登录，必须经 HTTP 访问，`file://` 双击打开已不可用。** 未登录访问任何页面都 302 跳到 `/login.html`。种子账号：`teacher01` / `stu001` / `stu002`，初始密码均为 `xiyun@2026`。鉴权实现见 `app/pages.py` 与 `app/common/decorators.py` 的 `page_login_required`，设计见 `docs/superpowers/specs/2026-09-15-page-auth-design.md`。
- **页面由 nginx 转发给 Flask 提供**（不再由 nginx 静态直供）：nginx 的 `location ~ ^/[^/]+\.html$` 改为 `proxy_pass` 到 8877，Flask 侧由无 `/api` 前缀的 `page_bp` 逐页注册路由并挂鉴权。`/login.html` 是唯一公开页；`/static/` 仍由 nginx 静态直供、公开。
- 纯前端页面无需构建、无需安装依赖，页面本身仍是纯静态 HTML（除 `pitch_comparison.html` 调后端外无后端请求）。
- 图表页依赖本地相对路径 `static/echarts.min.js`。
```

- [ ] **Step 4: 更新 `CLAUDE.md` 第三处——根路由变化**

找到「两份 `app-d.py` 的差异」表格中描述根路由的那一行说明，以及其后紧随的段落。在「其余（含 RMS / 频谱质心特征提取）完全一致。改后端前先确认改的是哪一份。」这句**之后**追加一段：

```markdown
> **2026-09-15 起，本目录 `app-d.py` 的根路由 `/` 不再能从 80 端口访问。** nginx 的 `location = /` 已改为 `return 302 /index.html`（首页自本次起受登录保护，不能再静态直供）。该根路由作为「通过 `app/` 包查 `users` 表」的数据库接入示例仍然保留，直连 `http://127.0.0.1:8877/` 可访问。
```

- [ ] **Step 5: 验证文档改动**

```bash
echo "--- DOC_ISSUES 条目数（应为 13 个编号条目 + 待核实）---"
grep -c "^## " DOC_ISSUES.md
grep -n "^## 13\." DOC_ISSUES.md

echo "--- CLAUDE.md 关键词 ---"
grep -c "13 条" CLAUDE.md
grep -c "page_login_required" CLAUDE.md
grep -c "page_bp" CLAUDE.md
```

预期输出：

```
--- DOC_ISSUES 条目数（应为 13 个编号条目 + 待核实）---
14
<行号>:## 13. 页面级鉴权文档完全未规定
--- CLAUDE.md 关键词 ---
1
1
1
```

（`grep -c` 数的是**匹配行数**不是出现次数：`grep -c "^## "` 为 `14` = 13 个编号条目 + `## 待核实`；后面三个关键词在 `CLAUDE.md` 里各自只出现在一整行中一次，所以都是 `1`。若某个数字偏大，说明同一行里重复写了该词或误改了别处。）

- [ ] **Step 6: 提交**

```bash
git add DOC_ISSUES.md CLAUDE.md
git commit -m "docs: 登记页面级鉴权文档空白，补 CLAUDE.md 运行方式变更

DOC_ISSUES 第 13 条：《6-登录与数据隔离方案》只定义了接口级鉴权，
未规定页面是否需要登录，按字面实现会得到「接口全鉴权、页面全裸奔」。
CLAUDE.md 补页面鉴权实现位置、file:// 失效、页面经 Flask 提供、
根路由 / 从 80 端口不可达三处。"
```

---

## 完成标准

全部任务完成后，以下应同时成立：

```bash
echo "=== 直连后端 8877 ==="
curl -s -o /dev/null -w '未登录 /dashboard.html   -> 302 (期望 302)\n' http://127.0.0.1:8877/dashboard.html
curl -s -o /dev/null -w '未登录 /login.html       -> %{http_code} (期望 200)\n' http://127.0.0.1:8877/login.html
curl -s -o /dev/null -w '未登录 /api/auth/me      -> %{http_code} (期望 401)\n' http://127.0.0.1:8877/api/auth/me

echo "=== 经 nginx 80 ==="
curl -s -o /dev/null -w '未登录 /dashboard.html   -> %{http_code} (期望 302)\n' http://127.0.0.1/dashboard.html
curl -s -o /dev/null -w '未登录 /login.html       -> %{http_code} (期望 200)\n' http://127.0.0.1/login.html
curl -s -o /dev/null -w '/static/echarts.min.js   -> %{http_code} (期望 200)\n' http://127.0.0.1/static/echarts.min.js
curl -s -o /dev/null -w '/.env                    -> %{http_code} (期望 404)\n' http://127.0.0.1/.env
curl -s -o /dev/null -w '/api/auth/me             -> %{http_code} (期望 401)\n' http://127.0.0.1/api/auth/me

echo "=== 冒烟全量 ==="
BASE=http://127.0.0.1:80 scripts/smoke_auth.sh && echo "✓ 全通过"
```

浏览器端手工验收（这是自动化测不到的部分）：登录 → 落到看板 → 右上角显示真实姓名与角色 → 12 个页面逐个点一遍都能打开 → 点登出 → 回登录页 → 直接敲 `/dashboard.html` 仍被弹回。
