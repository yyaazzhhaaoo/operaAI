# 戏韵AI — Python 依赖清单
# ==============================================================================
# 依据：../艺校_docs/11-部署与运维手册-V1.0.docx 第 2.3 节
# 生成环境：macOS (Intel x86_64) / Python 3.11.13 / 2026-09-10
# 部署目标：Ubuntu 22.04 / Python 3.11（见部署手册 1.1 节）
#
# 用法：
#   python3.11 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
#
# 系统依赖（pip 装不了，需先 apt 安装，见部署手册 2.1 节）：
#   libsndfile1  ffmpeg    # soundfile / librosa 解码音频所必需
#
# 说明：本文件钉死精确版本以保证可复现。升级请显式修改版本号后重装，
#       不要用 pip install --upgrade 让版本自行漂移。
# ==============================================================================


# ------------------------------------------------------------------------------
# 直接依赖 —— 应用代码直接 import 的包
# ------------------------------------------------------------------------------

# --- Web 服务：app-d.py ---
Flask==3.1.3                # Web 框架
flask-cors==6.0.5           # 跨域支持，配合前端页面直连

# --- 音频分析：app-d.py 的 extract_pitch / analyze ---
librosa==0.11.0             # 音高提取(pyin)、HPSS 人声分离、RMS/频谱质心
numpy==2.3.5                # 数值计算
scipy==1.17.1               # scipy.spatial.distance.euclidean，供 DTW 使用
fastdtw==0.3.4              # 老师/学生音高序列的 DTW 对齐

# --- 数据库：PostgreSQL（数据库设计-V1.1）---
psycopg2-binary==2.9.13     # 用 binary 版：免 pg_config，自带 libpq
                            # 注意：不要换成 psycopg2，源码编译需系统装 PostgreSQL
SQLAlchemy==2.0.52          # ORM，app/models/ 下的模型映射
pydantic-settings==2.15.0   # 读取 .env 配置，app/config.py

# --- 生产部署：gunicorn + gevent + celery + redis（部署手册 2.3 节指定）---
gunicorn==26.2.0            # WSGI 服务器，systemd 托管 xiyun-web
gevent==26.8.0              # gunicorn 异步 worker
celery==5.6.3               # 异步任务队列，音频分析转后台(xiyun-worker)
redis==8.1.0                # celery broker + 缓存


# ------------------------------------------------------------------------------
# 传递依赖 —— 由上面自动解析而来，一并钉死以防版本漂移
# ------------------------------------------------------------------------------

# --- Flask 生态 ---
blinker==1.9.0
click==8.5.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8

# --- librosa 音频分析链 ---
audioread==3.1.0            # 音频解码回退
cffi==2.1.1                 # soundfile 的 C 绑定
cloudpickle==3.1.2          # numba 序列化
decorator==5.3.1
joblib==1.6.0               # scikit-learn
lazy-loader==0.5
llvmlite==0.45.1            # ★ numba 的 LLVM 后端，必须钉死
msgpack==1.2.2
numba==0.62.1               # ★ librosa 的 JIT 加速，必须钉死
pooch==1.9.0                # 示例数据下载
pycparser==3.0              # cffi
scikit-learn==1.9.0
soundfile==0.14.0           # 依赖系统 libsndfile
soxr==1.1.0                 # 重采样
threadpoolctl==3.6.0        # scikit-learn
narwhals==2.26.0            # scikit-learn
platformdirs==4.11.8        # pooch

# --- Celery 生态 ---
amqp==5.3.1
billiard==4.2.4
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.3.0
kombu==5.6.2
prompt_toolkit==3.0.53
python-dateutil==2.9.0.post0
six==1.17.0
tzdata==2026.3
tzlocal==5.4.4
vine==5.1.0
wcwidth==0.8.3

# --- gevent 生态 ---
greenlet==3.5.5
zope.event==6.2
zope.interface==8.6

# --- HTTP 客户端链（pooch 拉取示例音频）---
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.19
requests==2.34.2
urllib3==2.7.0

# --- SQLAlchemy / pydantic 生态 ---
annotated-types==0.8.0      # pydantic
pydantic==2.13.5            # pydantic-settings
pydantic-core==2.46.5       # pydantic
python-dotenv==1.2.3        # pydantic-settings 读取 .env
typing-inspection==0.4.4    # pydantic

# --- 其它 ---
packaging==26.3
typing_extensions==4.16.0

# 示范库解析链路（Demucs）的 CPU-only 依赖。单独成文件的原因见该文件头部注释。
-r requirements-demucs.txt
